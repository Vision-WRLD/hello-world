# ── Secrets: never commit API keys ──
.env
.env.*
.dev.vars
.dev.vars.*
*.local
secrets.*
*.key

# ── Cloudflare / Wrangler ──
.wrangler/
.mf/

# ── Dependencies / build ──
node_modules/
dist/
.cache/

# ── OS / editor cruft ──
.DS_Store
Thumbs.db
*.log

.dev.vars*
!.dev.vars.example
!.env.example
